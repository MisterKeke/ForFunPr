<UserControl x:Class="Something.Desktop.Views.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="980">
    <Grid MaxWidth="1180" Margin="0,0,24,24" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="{Binding Title}" Style="{DynamicResource PageTitleStyle}" />
            <TextBlock Margin="0,7,0,0" MaxWidth="620" HorizontalAlignment="Left"
                       Text="{Binding Subtitle}" Style="{DynamicResource PageSubtitleStyle}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,0,0,14" Style="{DynamicResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Margin="0,0,10,0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Search note titles and bodies" />
                <ComboBox Grid.Column="1" Margin="0,0,10,0"
                          SelectedValue="{Binding SelectedArchiveFilter}"
                          SelectedValuePath="Tag">
                    <ComboBoxItem Content="Active" Tag="Active" />
                    <ComboBoxItem Content="Archived" Tag="Archived" />
                    <ComboBoxItem Content="All notes" Tag="All" />
                </ComboBox>
                <CheckBox Grid.Column="2" Margin="0,0,14,0" VerticalAlignment="Center"
                          Foreground="{DynamicResource TextDimBrush}"
                          Content="Pinned only" IsChecked="{Binding ShowPinnedOnly}" />
                <Button Grid.Column="3" Command="{Binding NewNoteCommand}"
                        Content="New note" Style="{DynamicResource PrimaryButtonStyle}" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="330" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{DynamicResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Margin="2,0,0,10" Foreground="{DynamicResource TextDimBrush}"
                               FontSize="12" Text="{Binding TotalLabel}" />
                    <ScrollViewer Grid.Row="1" MaxHeight="540" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <ItemsControl ItemsSource="{Binding Notes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Margin="0,0,0,8" Padding="11"
                                                HorizontalContentAlignment="Stretch"
                                                Command="{Binding DataContext.SelectNoteCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                CommandParameter="{Binding}"
                                                Style="{DynamicResource SecondaryButtonStyle}">
                                            <StackPanel>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Margin="0,0,5,0" Foreground="{DynamicResource WarningBrush}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                        <Setter Property="Text" Value="★" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <TextBlock Grid.Column="1" FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis" Text="{Binding DisplayTitle}" />
                                                    <Border Grid.Column="2" Margin="8,0,0,0" Padding="6,2"
                                                            Background="#247C8CFF" CornerRadius="8"
                                                            Visibility="{Binding IsArchived, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <TextBlock FontSize="10" Foreground="{DynamicResource AccentHoverBrush}"
                                                                   Text="Archived" />
                                                    </Border>
                                                </Grid>
                                                <TextBlock Margin="0,6,0,0" MaxHeight="38" TextWrapping="Wrap"
                                                           Foreground="{DynamicResource TextDimBrush}" FontSize="12"
                                                           Text="{Binding Preview, TargetNullValue=No body text}" />
                                                <TextBlock Margin="0,7,0,0" Foreground="{DynamicResource TextDimBrush}"
                                                           FontSize="10" Text="{Binding UpdatedAt, StringFormat={}{0:g}}" />
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Margin="8,40" HorizontalAlignment="Center"
                                       Foreground="{DynamicResource TextDimBrush}" TextAlignment="Center"
                                       Text="No notes match these filters."
                                       Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>
                    </ScrollViewer>
                    <Button Grid.Row="2" Margin="0,10,0,0" Command="{Binding LoadMoreCommand}"
                            Content="Load more" Style="{DynamicResource SecondaryButtonStyle}"
                            Visibility="{Binding CanLoadMore, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{DynamicResource CardStyle}">
                <Grid>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextDimBrush}" TextAlignment="Center"
                               Text="Select a note or create a new one.">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditorVisible}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Grid Visibility="{Binding IsEditorVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Margin="0,0,12,0" FontSize="17" FontWeight="SemiBold"
                                     Text="{Binding DraftTitle, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="12"
                                       Foreground="{DynamicResource TextDimBrush}" Text="{Binding SaveStatus}" />
                        </Grid>

                        <TextBox Grid.Row="1" Margin="0,12,0,0" MinHeight="350"
                                 AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                                 VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto"
                                 Text="{Binding DraftBody, UpdateSourceTrigger=PropertyChanged}" />

                        <Border Grid.Row="2" Margin="0,12,0,0" Padding="11"
                                Background="#20FF6B8A" BorderBrush="#70FF6B8A"
                                BorderThickness="1" CornerRadius="8"
                                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock VerticalAlignment="Center" TextWrapping="Wrap"
                                           Foreground="{DynamicResource ErrorBrush}" Text="{Binding ErrorMessage}" />
                                <Button Grid.Column="1" Margin="12,0,0,0" Padding="9,5"
                                        Command="{Binding ReloadActiveNoteCommand}" Content="Reload"
                                        Style="{DynamicResource SecondaryButtonStyle}"
                                        Visibility="{Binding HasConflict, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </Grid>
                        </Border>

                        <Grid Grid.Row="3" Margin="0,14,0,0">
                            <TextBlock VerticalAlignment="Center" FontSize="11"
                                       Foreground="{DynamicResource TextDimBrush}" Text="{Binding EditorTimestamp}" />
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button Margin="0,0,8,0" Padding="10,6" Command="{Binding FlushNoteSaveCommand}"
                                        Content="Save now" Style="{DynamicResource SecondaryButtonStyle}" />
                                <Button Margin="0,0,8,0" Padding="10,6" Command="{Binding TogglePinnedCommand}"
                                        Content="{Binding PinActionText}" Style="{DynamicResource SecondaryButtonStyle}" />
                                <Button Margin="0,0,8,0" Padding="10,6" Command="{Binding ToggleArchivedCommand}"
                                        Content="{Binding ArchiveActionText}" Style="{DynamicResource SecondaryButtonStyle}" />
                                <Button Padding="10,6" Command="{Binding DeleteNoteCommand}"
                                        Content="Delete" IsEnabled="{Binding CanDelete}"
                                        Style="{DynamicResource SecondaryButtonStyle}" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.ColumnSpan="3" Panel.ZIndex="20" Background="#9811131D"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="190" Height="4" IsIndeterminate="True" />
                    <TextBlock Margin="0,10,0,0" Foreground="{DynamicResource TextDimBrush}" Text="Working…" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
